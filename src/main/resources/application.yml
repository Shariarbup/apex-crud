server:
  port: 8087
  
spring:
  application:
    name: apex-crud
  datasource:
    url: jdbc:mysql://localhost:3306/apex_crud
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    org.springframework.security: DEBUG
